# masteruah

Ricardo@Ricardo-PC MINGW64 ~
$ history
    1  git config --global user.name "Ricardo GR"
    2  git config --global user.email "xrigaru@gmail.com"
    3  ls -al ~/.ssh
    4  ssh-keygen -t rsa -b 4096 -C "xrigaru@gmail.com"
    5  ls -al ~/.ssh
    6  ls
    7  ls -al .ssh
    8  eval $ (ssh-agent -s
    9  eval $ (ssh-agent -s)
   10  eval $(ssh-agent -s)
   11  ssh-add ~/.ssh/id_rsa_UAH
   12  ssh-add ~/.ssh/id_rsa_UAH
   13  ls -al ~/.ssh
   14  ls -al ~/.ssh
   15  ssh-add ~/.ssh/id_rsa_UAH
   16  clear
   17  clip < ~/.ssh/id_rsa_UAH.pub
   18  history


El repositorio está en :  https://github.com/xrigaru/masteruah.git 